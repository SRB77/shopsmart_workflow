# !/bin/bash 
# this is shebang which tells the system which interpreter or shell to use 
echo "Starting ..."
mkdir something
echo "Done" 

#First make this script executable using "chmod +x test.sh"
# Then run the file using ./test.sh tried and tested
